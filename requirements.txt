# Pinned to the set that actually runs on Python 3.13 (Windows). OCAtari pins
# numpy<2, but numpy<2 has no cp313 wheel and the source build segfaults, so we use
# numpy 2.1.3 (OCAtari runs fine against it in practice). ale_py supplies the Pong ROM.
numpy>=2.1,<3
scikit-learn
ocatari
ale_py>=0.10
# Rung 3 (learned Discrete-JEPA eye) additionally needs:
torch
pillow
matplotlib
